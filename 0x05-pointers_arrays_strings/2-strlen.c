#include <stdio.h>
#include "main.h"
/**
 * _strlen - Get the lenght of a String
 * @s : String 
 * Return: lenght of the String S
 */
int _strlen(char *s)
{
	int len;
	while(*s != '\0')
	{
		l++;
            	s++;
        }
    	return (len);
}
