#include"main.h"
#include<stdio.h>
/**
 * _isdigit - a function to check if input isa digit between 0 and 9
 * @c: A Number
 * Return: if 1 if yes and 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= 1 && c <= 9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
